#!/usr/bin/env node
var debug = require('debug')('server');
var app = require('../app');

app.set('port', process.env.PORT || 3000);

var server = app.listen(app.get('port'), function() {
  debug('Express server listening on port ' + server.address().port);
});
//MongoDB add, using mongoose

var mongoose = require("mongoose");
var dbUrl = "mongodb://heroku_rkk9fh3h:9ni9okpduof7mnqmfndv242cok@ds135456.mlab.com:35456/heroku_rkk9fh3ht";

mongoose.connect(dbUrl,{useMongoClient : true}, (err) => { 
  console.log("mongodb connected", err);
});

var Message = mongoose.model("App",{ status : String, why : String});

app.post('/App', (req, res) => {
  var appStat = new Message(req.body);
  appStat.save((err) =>{
    if(err)
      sendStatus(500);
    res.sendStatus(200);
  });
});

var io = require("socket.io").listen(server);

io.on("connection", function(socket){
  console.log("Client Connected");

  socket.on("stateChanged", function(state){
    console.log("State Changed: " + state);
    io.emit("updateState", state);
  });
});